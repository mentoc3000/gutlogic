import 'package:flutter/widgets.dart';

import 'gl_snack_bar.dart';

class InfoSnackBar extends GLSnackBar {
  InfoSnackBar({
    Key? key,
    required String text,
  }) : super(
          key: key,
          text: text,
        );
}
