import 'package:flutter/material.dart';

class GLPopupMenuItem<T> extends PopupMenuItem<T> {
  const GLPopupMenuItem({Key? key, T? value, Widget? child}) : super(key: key, value: value, child: child);
}
